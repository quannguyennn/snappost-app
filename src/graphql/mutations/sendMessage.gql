mutation sendMessage($input: NewMessageInput!) {
  sendMessage(input: $input) {
    id
    sender
    chatId
    content
    media
    mediaType
    sent
    tempId
    received
    createdAt
    senderInfo {
      id
      name
      nickname
      avatarFilePath
    }
  }
}
