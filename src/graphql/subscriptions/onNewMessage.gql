subscription onNewMessage($chatId: Float!) {
  onNewMessage(chatId: $chatId) {
    id
    sender
    chatId
    content
    media
    mediaType
    sent
    tempId
    received
    createdAt
    senderInfo {
      id
      name
      nickname
      avatarFilePath
    }
  }
}
