subscription onLikePost($postId: Float!) {
  onLikePost(postId: $postId) {
    id
    postId
    userId
    createdAt
    creatorInfo {
      id
      name
      nickname
      avatarFilePath
    }
  }
}
