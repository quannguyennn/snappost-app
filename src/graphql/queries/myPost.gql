query myPost($page: Float!, $limit: Float!) {
  myPost(page: $page, limit: $limit) {
    items {
      id
      mediasPath {
        filePath
      }
    }
    meta {
      itemCount
      totalItems
      itemsPerPage
      totalPages
      currentPage
    }
  }
}
